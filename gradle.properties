# Performance tuning (local builds)
# - Build cache: reuses outputs across incremental builds
# - Parallel execution: runs independent tasks concurrently
# - VFS watching: speeds up incremental builds by avoiding full file scans
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.vfs.watch=true

# Done to increase the memory available to Gradle (Fabric Loom can be memory-hungry).
org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.11
loader_version=0.18.2
loom_version=1.14.0-alpha.31

# Mod Properties
mod_version=1.0.0
maven_group=com.qb20nh.immersive_freezing
archives_base_name=immersive_freezing

# Publishing
modrinth_project_id=Llm6s2xf
curseforge_project_id=1410474

# Dependencies
fabric_version=0.139.4+1.21.11
modmenu_version=17.0.0-alpha.1